<?xml version="1.0"?>
<launch>

    <!-- Input Arguments -->
    <arg name="robot_description" default="$(command 'xacro $(find-pkg-share aims_gp7wc_support)/urdf/gp7wc.xacro')" description="[String] Path to URDF." />
    <arg name="rviz_config" default="$(find-pkg-share aims_gp7wc_support)/config/visualize_description.rviz" description="[String] Path to RViz config file." />

    <!-- Publish the robot state (tf transforms) -->
    <node pkg="robot_state_publisher" exec="robot_state_publisher" output="screen" >
        <param name="robot_description" value="$(var robot_description)" />
    </node>

    <!-- User Interaction -->
    <node pkg="rviz2" exec="rviz2" args="-d $(var rviz_config)" >
        <param name="robot_description" value="$(var robot_description)" />
    </node>

    <!-- <node pkg="joint_state_publisher" exec="joint_state_publisher">
        <param name="source_list" value="[/joint_states]"/>
        <param name="publish_default_positions" value="false"/>
    </node> -->

</launch>